package lambdas;

// Functional interface is an interface with a single abstract method
// it doesn't matter how much static and default methods had.
public interface Printer {
    void print(String message);
}
